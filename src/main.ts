import { createIcons, Gem, Home, SlidersHorizontal } from "lucide";
createIcons({
  attrs: {
    "stroke-width": 1,

    stroke: "#333",
  },
  icons: {
    Gem,
    Home,
    SlidersHorizontal,
  },
});
